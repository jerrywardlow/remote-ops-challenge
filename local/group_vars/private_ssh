---

# Ansible configuration for servers accessible only through SSH bastion host
# Groups: 'balancers', 'webservers'
ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q vagrant@172.17.17.54 -o StrictHostKeyChecking=no -i .vagrant/machines/ops4/virtualbox/private_key"'
